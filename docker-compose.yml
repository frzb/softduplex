version: "3"
services:
  softduplex:
    build: .
    environment:
      # Set to "true" to enable verbositiy
      VERBOSE: "false"
    logging:
      driver: "json-file"
      options:
        max-size: "200k"
        max-file: "10"
    volumes:
      - ./input:/input
      - ./output:/output
